server:
  port: 9090
  
spring:
  application:
    name: book-service
  cloud:
    consul:
        config:
         enabled: true
         default-context: application
         prefixes: config
         profile-separator: '::'
         format: YAML