server:
  port: 8082
  
spring:
  application:
    name: student-service
  cloud:
    consul:
        config:
         enabled: true
         default-context: application
         prefixes: config
         profile-separator: '::'
         format: YAML